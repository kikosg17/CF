{
  "name": "tesoreria-vision",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "node backend/server.js",
    "frontend": "cd frontend && npx vite --host 0.0.0.0"
  },
  "dependencies": {
    "concurrently": "^9.1.0"
  }
}
